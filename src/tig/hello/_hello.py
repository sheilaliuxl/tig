"""A test script in tig."""

from __future__ import absolute_import

def hello(name):
    """Say hello to a name."""
    return "Hello, " + name + "!"

def add_number(lhs, rhs):
    """Adds two numbers together."""
    return lhs + rhs

def mult_number(lhs, rhs):
    """Adds two numbers together."""
    return lhs * rhs
